#include <bits/stdc++.h>
using namespace std;
int main(){
    // int arr[7] = {4, 6, 7, 4, 7};
    // cout << arr[4];


    // struct Node{
    //     int data;
    //     Node *next;
    // };

    // queue<int>q;
    // q.push(40);
    // cout << q;

    // stack<int> s;
    // s.push(49);
    // s.push(45);
    // s.push(56);
    // while(!s.empty()){
    //     cout << s.top() << " ";
    //     s.pop();

    // }
    // s.push(100);
    // cout << s.top();
    // cout << endl;

    queue<int> q;
    q.push(1);
    q.push(2);
    q.push(3);
    q.push(5);
    while(!q.empty()){
        cout << q.();
        
    }
   
    return 0;
}